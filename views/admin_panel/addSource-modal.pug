block content
  div(id="addSource-modal", class="modal fade", role="dialog")
    div(class="modal-dialog")
      div(class="modal-content")
        div(class="modal-header")
          button(type="button", class="close", data-dismiss="modal") &times;
          h4(class="modal-title") New Source to be added to the Source Pool
        div(class="modal-body")
          
          form(id="newSource-form" action="#" method="post" onsubmit="event.preventDefault();")
            ul(class="nav nav-tabs nav-justified")
              li(class="active") 
                a(data-toggle="tab" href="#source-tab") Source
              li
                a(data-toggle="tab" href="#quality-tab") Quality
              li
                a(data-toggle="tab" href="#submit-tab") Submit
            
            div(class="tab-content")

              div(id="source-tab" class="tab-pane fade in active form-group well")
                // id: is autogenerated
                label(for="src-urlurl")
                  | Url
                input(type="text" name="src-url" class="form-control" placeholder="https://arxiv.org/abs/1503.02406" required)
                // urldate: "now"
                label(for="src-type")
                  | Type
                input(type="text" name="src-type" class="form-control" placeholder="paper / blog / forum thread / newsletter / ..." required)
                label(for="src-title")
                  | Title
                input(type="text" name="src-title" class="form-control" placeholder="Deep Learning and the Information Bottleneck Principle" required)
                label(for="src-authors")
                  | Authors (optional)
                input(type="text" name="src-authors" class="form-control" placeholder="Naftali Tishby, Noga Z. Zaslavsky")
                label(for="src-keywords")
                  | Keywords (optional)
                input(type="text" name="src-keywords" class="form-control" placeholder="AI, Machine Learning, Very interesting stuff")
                label(for="src-date")
                  | Date
                input(type="date" name="src-date" class="form-control" required)
                label(for="src-abstract")
                  | Abstract
                textarea(name="src-abstract" class="form-control" rows="5" required)
                label(for="src-ptrs")
                  | Source-pointer(s) (optional)
                select(name="src-ptrs" class="form-control" size="2" multiple)
                  // TODO: load all available sources
                  each source in sources
                    option(value=source._id)
                      |"
                      = source.title 
                      |" by 
                      = source.authors

              div(id="quality-tab" class="tab-pane fade form-group well")
                label(for="src-q-subjectRelevance")
                  | Subject Relevance 
                  span(class="glyphicon glyphicon-info-sign" title="How generally useful is the source expected to be?\n(This takes into account how relevant the source is expected to become concerning queries on the literature research subject.)" required)
                input(type="number" name="src-q-subjectRelevance" class="form-control" min="0", max="7")
                label(for="src-q-informativeContent")
                  | Informative Content 
                  span(class="glyphicon glyphicon-info-sign" title="How much formal information is actually present in the source?" required)
                input(type="number" name="src-q-informativeContent" class="form-control" min="0", max="7")
                label(for="src-q-context")
                  | Context 
                  span(class="glyphicon glyphicon-info-sign" title="How precisely does the source motivate its findings and respective context?\n(This mainly focuses on cross references, where many references indicate a holistically covered research. The more concise a source appears, the more in-depth this source can be explored without running into blind ends not mentioned / thought of by the author(s).)" required)
                input(type="number" name="src-q-context" class="form-control" min="0", max="7")
                label(for="src-q-illustration")
                  | Illustration 
                  span(class="glyphicon glyphicon-info-sign" title="How well is information processed to make it easy to understand / put into context?\n(This (compared to conciseness) mainly focuses on the way the information is being presented concerning utility and understandability. The more illustration a source uses to explains its motivation, the easier the source can be used by non-domain-experts." required)
                input(type="number" name="src-q-illustration" class="form-control" min="0", max="7")

              div(id="submit-tab" class="tab-pane fade form-group well")
                label(for="src-comment")
                  | Comment (optional)
                textarea(name="src-comment" class="form-control" rows="12")
                input(type="submit" class="btn btn-success" value="Enter Source" onclick="saveSrc();")
          
        //- div(class="modal-footer")
        //-   button(type="button", class="btn btn-default", data-dismiss="modal") Close
        